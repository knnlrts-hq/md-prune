Please use your skills to help me brainstorm the following CLI tool called md-prune that parses a markdown file and outputs a new markdown file (2 options: direct to file or to stdout which can then be piped into a file) that only keeps the bolded (**) and/or highlighted (==) sentences/text elements.
Inline math, lists, backtick-bracketed text `text`, etc are considered as part of the sentences/text. Make CLI parameters to optionally:
- keep headings/titles yes/no (default yes)
- keep images yes/no (default yes)
- keep tables yes/no (including raw html tables) (default yes)
- keep code blocks yes/no (default yes)
- keep display math blocks yes/no (default yes)
- keep callout blocks yes/no (default yes)
- ...
Brainstorm the most important aspects together with me and touch on questions regarding the best approaches concerning usability and UX, robustness, etc.
Use the rust language and toolchain for this project. Make sure to fully read CLAUDE.md for the rust development best practices. Determine the best crates to integrate if appropriate. The code should be well-documented and easy for a rust beginner to follow along and understand what is happening.
Make sure that the project has a well-structured README.md on how to use the tool, as well as build the build from source on the main operating systems.
There are example input markdown files provided in references/. Make sure they are used for testing purposes.
Once we have brainstormed everything, I need your help to write out a comprehensive  implementation plan.
Assume that the engineer has zero context for our codebase and questionable taste. document everything they need to know. which files to touch for each task, code, testing, docs they might need to check. how to test it.
give them the whole plan as bite-sized tasks. DRY. YAGNI. TDD. frequent commits.
Assume they are a skilled developer, but know almost nothing about our toolset or problem domain. assume they don't know good test design very well. 
please write out this plan, in full detail, into docs/plans/.
